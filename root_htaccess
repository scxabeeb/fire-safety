<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Redirect everything to the Firesfty/public folder, UNLESS it already starts with Firesfty
    RewriteCond %{REQUEST_URI} !^/Firesfty/
    RewriteRule ^(.*)$ Firesfty/public/$1 [L]
</IfModule>
